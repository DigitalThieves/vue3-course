jest.mock('@/services/api-client')
beforeEach(() => {
  jest.clearAllMocks()
})

describe('Testing Header and Footer Components', () => {
  it("Should have a test, but doesn't... To be continued I gues...", () => {
    expect(true).toBeTruthy()
  })
})
